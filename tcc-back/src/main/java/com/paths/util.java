package com.paths;

public class util {
	public static String formataReq(String req) {
		return req.substring(7, req.length() -1);
	}
	public static String formataReq2(String req) {
		return req.substring(9, req.length() -1);
	}
}
